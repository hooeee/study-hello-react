// boolean : True or False

const amIFat = true;
console.log(amIFat);

// undefined = DataType / 값이 정의되지 않음
// null = DataType / 값이 없음

const Null = null;
console.log(Null);

let Undefine;

console.log(Undefine);